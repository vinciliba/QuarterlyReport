<!doctype html>
<html>
<head>

  <style>
     body{font-family:Arial;margin:40px}
     h1{color:#1464e1}
     table{border-collapse:collapse}
     th,td{border:1px solid #999;padding:4px 8px;text-align:right}
     th{text-align:left}
  </style>
</head>
<body>
  <h1>Quarterly report – {{ quarter }}</h1>

  <p>Hello <strong>{{ user }}</strong>, this was rendered at
    {{ now.strftime('%Y-%m-%d %H:%M') if now is defined else "n/a" }}.</p>

  <h2>Uploads</h2>
  <table>
    <tr><th>Alias</th><th>Rows</th></tr>
    {% for alias, rows in uploads %}
      <tr><td>{{ alias }}</td><td>{{ rows|int }}</td></tr>
    {% endfor %}
  </table>
</body>
</html>
